specVersion: "v1beta"
serviceId: "react-task-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "react-task-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
